import React from 'react'
import ReactDOM from 'react-dom/client'
import ActivityTracker from './activity-tracker.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ActivityTracker />
  </React.StrictMode>
)
